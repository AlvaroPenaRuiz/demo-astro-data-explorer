# demo-astro-data-explorer
This is a demo project to get some data from space and do a brief analisys and plotting.

# Dev
To run in dev mode the proyect you need docker to be installed and then run from the base directory:
```
docker compose -f docker-compose.dev.yaml up --build
```